TaskMaster Backlog:
  Input & AI:
    ✔ Add Settings toggle to control AI task enrichment on creation (default off) @high
    ☐ Add command-style task input (create/reschedule/complete/query) with clear confirmation before apply @high
    ☐ Add prompt quality/relevance tuning loop (track accept/edit/dismiss signals and improve suggestion quality over time) @high
    ☐ Add bulk completion from natural-language recap ("I did X, Y, Z today") with preview + confirm @high
    ☐ Add disambiguation prompts when command target is unclear (multiple matching tasks) @high
    ☐ Expand "Plan my day" into a PLAN hub (day/week/month/year) with wizard + editable draft before save @high
  Task List Behavior:
    ☐ Add smart auto-reposition for recurring tasks on completion (with smooth animation) @high
    ☐ Add pin-to-top tasks section that overrides group/sort/filter ordering @high
  Explore:
    ✔ Add stronger Explore suggestion trigger guards (debounce, request cooldown, stale-response protection, in-flight dedupe) @high
    ✔ Add Explore suggestions cache with profile/prompt invalidation-aware keys to reduce repeated AI calls @high
    ☐ Add suggestion type labels and filters (task/recurring/project/plan) @medium
    ☐ Add Explore guidance controls (difficulty, duration, category, free-form direction) @medium
    ☐ Add comfort-zone slider + feedback weighting for suggestion relevance tuning @medium
    ☐ Add "guidance everywhere, never pushy" pass for major suggestion entry points @medium
  Projects:
    ☐ Add "Convert task to project" flow that scaffolds subtasks and carries context @medium
    ☐ Let accepted Explore suggestions start project creation flow directly @medium
    ☐ Add goal-to-milestone decomposition flow (big goals -> small actionable steps) @medium
    ☐ Decide whether Projects becomes PLAN hub (rename + expand) or PLAN lives alongside Projects @medium
  Onboarding:
    ☐ Add "I don't know, help me with this" self-discovery branch with guided prompts @medium
    ✔ For predefined guest templates, skip AI starter-task generation and seed deterministic canned persona/tasks with fresh date rebasing @high
  Core Experience:
    ☐ Add 30-60 second daily check-in flow (review + tomorrow plan) @medium
    ☐ Add reward-style personalization controls without changing core task flow @low
    ☐ Audit and enforce task-manager standalone quality independent of gamified layer @medium
  Reliability & Platform:
    ✔ Add AI usage guardrails (global queue, per-feature cooldowns/priorities, 429 backoff, and shared cache fallbacks) @high
    ✔ Implement optional server-side Claude gateway path (`/api/claude`) with client env switch (`VITE_AI_GATEWAY_URL`) @high
    ✔ Add Phase-4 adaptive AI controls (quality tiers, session request budget cap, and richer runtime telemetry) @high
    ✔ Add admin-toggle AI runtime floating strip below top navigation (live budget + request telemetry) @medium
    ✔ Add Claude session token counters + explicit Cursor usage availability note in AI debug strip @medium
    ✔ Add approximate Claude USD cost estimate in AI debug strip (configurable per-1M token rates) @medium
    ✔ Add "last AI action" usage snapshot in debug strip (feature/status/tokens/estimated cost) @medium
    ☐ Set up first deployment host (Vercel or equivalent) and configure `ANTHROPIC_API_KEY` + `VITE_AI_GATEWAY_URL=/api/claude` @high
    ☐ Move AI API calls behind a server endpoint before wider friend testing (no client-exposed API key) @high
    ☐ Package iOS beta via Capacitor + TestFlight for friend testing before App Store release @high
    ☐ Ensure AI-off/manual-first parity across core flows (no dead ends) @high
    ☐ Further optimize resource usage across Plan and task views (reduce repeated AI calls, add tighter cache reuse, and lower unnecessary rerenders) @high
    ☐ Run mobile-first optimization pass for core daily workflows @medium
    ☐ Add configurable reminder notifications (phase 1: simple daily check-in) @low



Template for TODO extension, don't edit:

Projects:
  ☐ Anything with a colon at the end of the line is a project
  ☐ Projects will show some statistics next to them @1h
    ✔ By default those statistics are the number of pending todos and the sum of their time estimates @30m
  Nested:
    ☐ You can nest projects inside each other and fold them

Todos:
  You can write plain text notes/descriptions wherever you want
  New:
    ☐ Press Cmd/Ctrl+Enter to add a new todo
  Done:
    ✔ Press Alt+D to mark a todo as done
    ✔ Press it again to undo the action
  Cancelled:
    ✘ Press Alt+C to mark a todo as cancelled
    ✘ Press it again to undo the action
  Tagging:
    ☐ You can add tags using the @ symbol, like this @tag
    ☐ There are some special, customizable tags: @critical @high @low @today
  Timekeeping:
    ✔ Completed todos can show a timestamp @done(17-11-03 10:42)
    ☐ Press Alt+S to mark a todo as started @started(17-11-03 10:42)
      ✔ Now it will show the elapsed time @started(17-11-03 10:42) @done(17-11-03 20:11) @lasted(9h29m)
    ☐ You can provide time estimates for your todos @1h30m
      ☐ We are even doing some natural language processing @est(1 day and 20 minutes)

Formatting:
  You can format text in a markdown-like fashion
  Bold:
    ☐ Use asterisks for *bold*
  Italic:
    ☐ Use underscores for _italic_
  Strikethrough:
    ☐ Use tildes for ~strikethrough~
  Code:
    ☐ Use backticks for `code`

Archive:
  ✔ You can archive finished todos here
  ✔ Congratulations, you are now a Todo+ master!
